[project]
name = "artify-common"
version = "0.1.0"
description = "Shared utilities and config for ArtifyAIv.1"
requires-python = ">=3.12"
dependencies = [
  "pydantic-settings>=2.4",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
